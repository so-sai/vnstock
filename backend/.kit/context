# .kit Project Context
# Generated: 2026-04-05 17:17:40 | Root: PTCK_VNSTOCK
# Cognition-Version: 11

- FIX_BUG_BUNDLE_v1: 1. VNSTOCK_AUTH: Bypassed auth.py via explorer providers. 2. FOREIGN_FLOW: Implemented Snapshot Accumulation for broken endpoints. 3. PATH_GUARD: Added Path Sentinel in src/engine/*.py. 4. SQLITE_LOCK: Enabled WAL Mode & 60s timeout. 5. STRICT_314: Used Frozen Dataclasses. 6. PS_FIX: Use single quotes for python -c commands. 7. API_V240: Use history() instead of price_historical() in explorer.
- API_DEFENSE_ELITE: Request Collapsing (Singleton), Multi-tier Caching (RAM/Disk), Negative Caching & Circuit Breaker (Backoff on 429/5xx) to prevent IP Ban during data harvesting.
- Kiến trúc API Elite BẮT BUỘC có 3 lớp bảo vệ Quota: 1. Singleton Request (vượt Burst Limit). 2. Multi-layer Cache (bảo vệ Daily Quota). 3. Negative Caching 60s cooldown (chống Ban IP do spam lỗi). Lưu ý: Kiến trúc này chỉ giải quyết Volume-based Limits, không bypass được WAF Bot Detection.
- Project 'PTCK_VNSTOCK' initialized and integrated into .kit cognitive system (vv1.2.3.3.1-ULTRA-GOLD).
- kit startup begins with kit recall
- Daily: recall & verify. Weekly: stats & doctor. Monthly: seal.
- Structural signals from Vantage (v1.2.4) are physical truth. No hallucinations.
- Markdown is volatile. SQLite is Truth. Log friction via kitf.ps1.
- AGENTS.md is the root lighthouse; .kit/ is the private cognitive vault.
- L1 (Fast Guard) -> L2 (Structural/Vantage) -> L3 (Cognitive/SQLite).
- Minimize static docs. Use 'kit --help' for syntax and 'kit recall' for rituals.
