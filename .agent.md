# PTCK_VN AGENT MANIFEST
# Auto-generated by `init-agent`

## EXECUTION ENGINE
Executable Absolute Path: `python ptck.py`

## USAGE PROTOCOL
AI Agent hãy dùng Terminal Tool để thực thi trực tiếp các lệnh CLI bên dưới.
Ưu tiên cờ `--verbose-lang compact` khi phân tích dữ liệu để tiết kiệm Context Window.

## CLI COMMANDS MANIFEST (67 COMMANDS)
1. `absorption-detector` : Phát hiện Cân bằng Hấp thụ thị trường (SDI + Volume Profile PCA)
2. `backfill-history` : Khôi phục dữ liệu lịch sử cho mã thiếu (Backfill)
3. `backfill-macro` : Backfill 1y historical data cho PTD macro tickers
4. `backfill-regime` : Backfill regime_history cho ngày thiếu trong daily_ohlcv
5. `break-glass` : Break-Glass Protocol — override Hard Shutdown
6. `break-glass cancel` : Hủy ticket
7. `break-glass request` : Yêu cầu override ticket
8. `break-glass status` : Xem trạng thái lock
9. `break-glass verify` : Xác thực ticket
10. `check-calendar` : Kiểm tra lịch lễ so với nguồn online
11. `cleanup` : Dọn dẹp định kỳ: telemetry, state files
12. `clear-crisis` : Xóa crisis_cooldown + stress gate — reset trạng thái khủng hoảng thủ công
13. `confidence` : Bộ tự đánh giá độ tin cậy
14. `daily-close` : Chạy daily closer pipeline
15. `daily-update` : Cập nhật dữ liệu EOD
16. `data-quality` : Đánh giá độ tin cậy dữ liệu (TẦNG 0)
17. `db` : Database operations
18. `ddi` : Delta Divergence Index — Δ_SA filter
19. `eod-run` : EOD Runner — retry + idempotent + SQLite lock
20. `erl-scan` : Entity Resilience Layer — quét sức khỏe 1.514 mã + Whitelist Top 30
21. `final-decision` : Bộ não quyết định cuối cùng
22. `flow-map` : Bản đồ Dòng vốn Liên thị trường 4 Tầng
23. `gap-analyzer` : Kiểm toán dữ liệu - phát hiện missing symbols/dates
24. `gold` : Gold information
25. `governor-report` : Phân rã đóng góp 3 mô hình vào DecisionGuard
26. `group-influence` : Bộ đo ảnh hưởng nhóm trụ
27. `index-decompose` : Phân tích chỉ số thị trường thống nhất
28. `macro` : Macro Governor Gatekeeper — Two-Tier Architecture (Tier 1)
29. `market` : Market snapshot
30. `paper` : Paper Trading Engine — Live vs Backtest simulation
31. `phase4` : Phase 4 CAS-DSM — Capitulation Detector + Scale-In + Abortion
32. `phase5` : Phase 5 UAT — Paper Trading Simulation
33. `prediction-registry` : Nhật ký dự báo — ghi log RS audit + đo kết quả sau 5/20/60 ngày
34. `quantstats` : QuantStatsBridge — Live Calibration Engine
35. `regime` : Regime analysis chi tiết
36. `registry` : Param Fingerprint Registry — tra cứu params_hash theo regime
37. `rejected-signals` : Rejected Signals Archive — Kho lưu tín hiệu bị từ chối
38. `rejected-signals eod-update` : Cập nhật simulated_exit cho các signal chưa có
39. `rejected-signals list` : Liệt kê rejected signals gần đây
40. `rejected-signals stats` : Thống kê theo rejection_reason
41. `report` : Báo cáo (weekly/monthly/daily)
42. `restore-backup` : Khôi phục trạng thái alert từ backup gần nhất
43. `rotation` : Góc xoay chuỗi cung ứng châu Á: VN vs KOSPI+TAIEX+DXY
44. `rs-audit` : Audit Top RS — phân tích nguồn gốc sức mạnh
45. `sandbox` : Phase 5 Dashboard — Real-time Paper Trading
46. `sbv-update` : 5-step recovery: backup → fixtures → pytest → scrape → clear alert
47. `scan` : Elite scanner
48. `sel` : Structure Evolution Layer — W1 Wasserstein + Survival Mode
49. `serve` : Khởi động server
50. `silver` : Silver information
51. `snapshot` : Ảnh chụp thị trường duy nhất
52. `snapshot-index` : Xem lịch sử snapshot_index.json
53. `stale` : StalePositionManager — quản lý vốn kẹt + escrow
54. `stale escrow` : Xem escrow balance
55. `stale reclaim` : Reclaim một lớp stale
56. `stale status` : Xem trạng thái stale layers + escrow
57. `stale writeoff` : Write-off LIFO (campaign mới nhất trước)
58. `status` : Kiểm tra sức khỏe hệ thống
59. `strength-discrimination` : Phân tách xung lực nội tại vs ép trụ
60. `structural` : Bộ phát hiện lệch cấu trúc thị trường
61. `telemetry` : Telemetry & reputation
62. `twap` : TWAP Executor — thanh lý stale positions
63. `twap plan` : Xây dựng kế hoạch TWAP
64. `twap resume` : Resume TWAP sau mất mạng
65. `twap run` : Thực thi TWAP
66. `twap status` : Xem trạng thái TWAP
