# AGENT COGNITIVE BOOTLOADER

**STATUS:** Kit v1.2.5 GOLD | Phase 3 Complete | Full-Stack Quant OS

**Startup:** `python backend/src/utils/sentinel_check.py; kit recall`. **Sentinel Law:** All `.py` files MUST include `_hydrate_path()` v2.2 anchored on `AGENTS.md + backend is_dir`. **Memory:** `.kit/local_brain.db` is truth; markdown is volatile. **Friction:** `kit learn --tag pattern|friction`. **CLI:** Always use `ptck.py`; never import modules directly.

4. Earlier: `final-decision` DUNG NGOAI confirmed via XAI trace — structural VỠ CẤU TRÚC 1/3 pillar + DDI healing illusion + Interbank VETO, confidence 31-32%.

**Session Aug 04 (Zero-Hallucination Campaign & Technical Maturity):** Bắt đầu từ LỖ HỔNG Density-vs-Provenance — `DataIntegrityAuditor` đo density/magnitude nên dữ liệu bịa (vừa đầy đủ vừa đúng độ lớn) bỏ qua **provenance** → trá hình nguồn thật trong DB. Đã triệt tiêu hoàn toàn ảo giác dữ liệu:
1. **Niêm phong Zero-Hallucination** (commit `6cd60f6`): `_generate_synthetic_base` vĩnh viễn trả `[]` (Sắc lệnh 2026-08-04). Nguồn thật chết ⇒ `NO_DATA` → Governor ép **ĐỨNG NGOÀI** (bảo toàn vốn thô), không bao giờ bịa. `financial_facts.db` thêm cột `is_synthetic` (migration idempotent ALTER) + `write_batch`/`write_fact` lưu `source`/`is_synthetic` (No Provenance = No Trust). `DataIntegrityAuditor` giờ trừ mọi quý chứa fact synthetic khỏi density → không nhận `PERFECT` nhờ số vẽ. Tests mới: +2 provenance auditor, +3 NO_DATA/provenance crawler. **607/607 PASS**. `vn50_seeder.py` release khỏi hoãn commit (LAW-007).
2. **Purge 352 bản ghi dữ liệu giả**: xóa rows STANDARD kỳ 2025Q3→2026Q2 ingested < 08-04 03:00 (gồm 5 row `TOTAL_DEBT=25e12` HDB/MBB/VCB/VPB/IJC + các 9.4e12/9.85e12...). Giữ 612 rows STANDARD re-crawl thật (BSR/GVR/HVN/FPT/HPG...) + 123 rows BANK thật (NII/CUSTOMER_DEPOSITS). DB sau purge: 0 bản ghi lệch scale tỷ đồng, 0 synthetic còn sót.
3. **Đính chính CLI chuẩn VN20**: lệnh đúng là `python ptck.py vn20` — flag `--rebalance` KHÔNG tồn tại trong parser vn20 (chỉ `--top-n`/`--quiet`, ptck.py:4231). Sector Gate vốn tự chạy trong `run_vn20_filter`.
4. **Fix FutureWarning pandas** (`vn20_quant_filter.py:364`): `df.groupby("symbol")["close"].pct_change(fill_method=None)` — loại bỏ deprecation của pandas về `fill_method` mặc định. Test vn20 sạch warning pct_change; full suite **607/607 PASS** (64s).
5. **Kết quả phân bổ VN20 hiện tại** (`ptck.py vn20`, data 2026-08-03): Funnel 55→11→10→10→9; **9/9 Ngân hàng** (HDB/ACB/MBB/TPB/VIB/AGT/STB/LPB/BID) qua 4 tầng, MoS 40-79%, pha RECOVERY. Sector Gate khống chế nhóm Ngân hàng ở **~49.98-50%**, **50% Cash/Defensive (ĐỨNG NGOÀI)** — bắt đáy từng phần, giữ đạn phòng thủ.
6. **Tier 2 Industry-Relative Percentile Gate** (commit `7f26943`): `tier2_governance_shield` upgraded — dual-condition: pass if ratio ≤ 25% (safe harbor) OR ≤ sector P75 (industry-relative). `_compute_sector_receivables_p75` pre-computes per-sector P75 from universe. 4 new tests pass. Rationale: Static 25% cap caused Type II Error against B2B/IT (FPT 67.2% receivables = structural). **Backtest harness** (`vn20_backtest.py`): quarterly A/B from 2022Q1-2026Q2. Results: Static CAGR +2.0% Sharpe 0.52 vs Percentile CAGR +1.8% Sharpe 0.43. Percentile gate opens 20 extra stocks (BCM/BSI/DGC/DXG/GMD/HAH...) but most underperform — net -0.2% CAGR. FPT NOT in uplift (blocked at T1: D/E=609M corruption since 2025Q3, not T2).
7. **health_ratios purge + Solo sector fallback** (commit `7499aaf`): Purge 19 corrupt D/E rows (FPT/HPG/HVN/MWG/VHM) from health_ratios, recalculated from clean financial_facts. FPT D/E restored to 0.41-0.98. Solo sector fallback: `_compute_sector_receivables_p75` returns max(ratios) instead of None for sectors <3 stocks — FPT now passes T2. Backtest rerun: Percentile CAGR +1.9% Sharpe 0.46 (was 1.8%/0.43), B-A gap closed to -0.0% (neutral). FPT in B for all 17 quarters.
8. **Full market P75 scan + Cascade delete** (commit `d37efbc`): `_compute_sector_receivables_p75` expanded from universe (55 symbols) to full market scan (1,555 symbols from symbol_industry) for maximum sector peer context. 9 sectors with <3 stocks use P100 fallback. `write_batch()` now cascade deletes corresponding health_ratios when financial_facts are purged — prevents silent data desync. `FinancialFactsDB.cascade_delete_health_ratios()` method added for bulk operations.
9. **Liquidity Recovery Index (LRI)** (commit `c322b54`): New module `backend/src/governor/liquidity_recovery_index.py` — 5-component leading liquidity indicator [0,1]: S_Interbank(30%) + S_USDVND(25%) + S_OMO(20%) + S_FII(15%) + S_Breadth(10%). 3 regimes: AGGRESSIVE(>=0.8), PROBE(0.3-0.8), DEFENSIVE(<0.3). Replaces binary VETO with Dimmer Scaling — graduated allocation instead of all-or-nothing. Integrated into `decision_guard.py` via `he_so_giam_ty_trong` modulation. Live test: LRI=0.2823 DEFENSIVE (Interbank 5.72% + USD/VND 26291 stress, but Breadth 68.3% strong — classic Kinh tế thực vs Thanh khoản Tài chính divergence). 17 TDD tests. 631/631 PASS.
10. **LRI v3 Regime-Aware Bayesian Bound + LAW-010** (2026-08-04): Upgraded LRI from static Policy Corridor (3.0%-7.5%) to Regime-Aware Bayesian architecture. New `backend/src/governor/regime_classifier.py` — 3-state Gaussian HMM (EXPANSION/NORMAL/CONTRACTION) trained on 932-day interbank history. Fuzzy Adaptive Bounds: `P90_eff = alpha * Observed + beta * sum(P(Regime_k) * Hist_P90_k) + gamma * Policy`. Alpha scales with sample size (0 at n=0, 0.5 at n=250+). Regime transition probabilities enable smooth LRI evolution across economic cycles. **LAW-010 (Regime Invariance Principle)**: "Mọi bằng chứng tài chính không được đánh giá bằng giá trị tuyệt đối; bắt buộc chuẩn hóa theo phân phối của Regime tại thời điểm quan sát." Live: LRI=0.5545 PROBE (S_Interbank=0.049 via Bayesian bounds P10=3.39 P90=5.84). 37 LRI tests. 651/651 PASS.
11. **MacroLagEngine + LAW-009** (2026-08-04, commits `b77be5a`+`6f62490`): Step 2 of Causal DAG Architecture. New `backend/src/governor/macro_lag_engine.py` — exponential time-decay for sector macro signals. `TransmissionParameters` per sector (STEEL: lag 3-21d HL=15d, BANK: lag 15-60d HL=45d, RE: lag 30-120d HL=90d). Formula: `S_eff = Σ M(t-d) × 0.5^(d/HL) / Σ 0.5^(d/HL)`. Reconstructs historical M vectors from raw indicator DB. `CompanyExposureModifier` applies per-company lag deltas (HPG: -8d vertical integration, NKG: +10d EAF fragmented, VCB: +10d state bank). `BayesianMandate` +4 fields: `sector_macro_score_effective`, `sector_macro_lr_effective`, `signal_deficit`, `lag_half_life`. **LAW-009 (Causal Delay & Interaction Principle)**: "Mọi tín hiệu vĩ mô có độ trễ truyền dẫn đặc trưng theo ngành; tín hiệu hiệu quả là tổng có trọng số suy giảm hàm mũ của lịch sử, không phải giá trị hiện tại." CLI `final-decision` now shows raw + effective + deficit per sector. 25 new tests. 726/726 PASS.

**Session Jul 31:** Vietstock BCTT tab free — 46 norms (CASH_EQUIV, RECEIVABLES, INVENTORY, LONG_TERM_DEBT, COGS) via 3 endpoints, 9 periods, merge financeinfo 4q + BCTT 9q. Cross-check tool detect 1Q label drift in CafeF BCM data. Full suite 406/406 PASS.

**Session Aug 03 (Post-Market Campaign):** Locked 2026-08-03 decision **DUNG NGOAI** (100% Cash) — verified real (not data artifact) after volume backfill. Fixed `volume_accumulated` mapping (KBS 4.0.5 renamed `total_trades`; commit `63a060f`) → 330 stocks >50k vol. Then post-market:
1. **PIT Loader refactor** (commit `4706b91`): `L3ValuationLoader`/`L4BehaviorLoader`/`load_health` + `CompanyHealthV2` now take `target_date`; financial queries gated `period/date <= target` — killed time-collapse (was reading `MAX(period)`/latest volume profile regardless of sample date). MacroStateClassifier: Bayesian Marginalization replaces static defaults (VIX/NASDAQ/breakeven) so pre-2023 feeds no longer force INFLATION_SHOCK. Harness `ab_test_models.py` point-in-time: M2-M3 gap moved 0.0144→0.0116, 2022 cycle reaches 61-62% acc all models; 2023-24 M2/M3 still NO_DATA (valuation only 2024Q4+, volume_profile only 2026-07+) — data gap, not code.
2. **XAI Causal Override Trace** (commit `297425d`): new `src/utils/xai_override_trace.py` prints RAW signal → VETO chain (LAW-006 structural, DDI gate, interbank, index-reality) → confidence penalty → final capital action when final ≠ raw; `orchestrator.py` captures `quyet_dinh_raw_daily`. No silent overrides on CLI. Live 08/03 verified.
3. **SBV historical interbank crawler CLOSED (Scenario B)**: Probe Playwright 1-pass 03/08 to `sbv.gov.vn/lãi-suất1` → HTTP 200, no Cloudflare, **0 file-download links (.xls/.doc/.pdf)**. SBV runs on Liferay CMS; interbank rate history only exists as current-day HTML table, no downloadable time-series archive for 2021-2022. **Permanent standard:** keep Bayesian Marginalization (commit `4706b91`) — 2021-2022 interbank = clean unknown, never fabricated. Do NOT rebuild `sbv_importer.py` crawler; source does not exist in fetchable time-series form.
4. Earlier: `final-decision` DUNG NGOAI confirmed via XAI trace — structural VỠ CẤU TRÚC 1/3 pillar + DDI healing illusion + Interbank VETO, confidence 31-32%.