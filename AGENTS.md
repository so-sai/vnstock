# AGENT COGNITIVE BOOTLOADER

> **STATUS:** Kit v1.2.4 GOLD | Infrastructure Hardened
> **WARNING:** This file is the operating constitution. `kit` is the authority.
> **SENTINEL LAW:** All entry-points MUST use the `_hydrate_path()` v2.0 protocol (Bulletproof Sentinel) to ensure absolute portability.

## 🧭 1. Mandatory Startup Sequence

Before taking action in this repository:

```bash
python src/utils/sentinel_check.py && kit recall && kit-vantage verify .
```

1. Run the startup sequence above exactly as written.
2. **SENTINEL LAW:** Every executable .py file MUST include the `_hydrate_path()` v2.1 (Anchor Fix) header.
3. Only open docs for syntax/reference after memory hydration.
3. If `kit` is missing, check your Global PATH.

## 🛠️ 2. Minimal Navigation

1. `AGENTS.md` (Constitutional Laws)
2. `.kit/docs/reference.md` (Exact Syntax)
3. `.kit/brain.db` (The local brain SQLite store)
4. `.kit/scripts/kitf.ps1` (Friction Logger)

## 🧠 3. Iron Laws Of Memory

1. **Markdown is volatile.** `.kit/brain.db` is the source of truth.
2. **Log Friction.** Use `.kit/scripts/kitf.ps1` or `kit learn --auto` to capture decisions.
3. **Inspect First.** Never guess paths or symbol structure.
4. **Evidence over Truth.** In v1.2.4-EXPERIMENTAL mode, treat structural signals as observations to be reasoned over, not as final truths. Log all structural noise or drifts into the `.kit` system.

## ⚡ 4. Fast Start

- `kit recall` -> Hydrate memory
- `kit context` -> View local awareness
- `kit learn --auto` -> Capture friction/insight

<!-- GENERATED BY KIT START -->
<!-- GENERATED BY KIT END -->
